import{escapeText,normalizeEmail}from'../utils/security.js';const roles=['admin','consejero','sindico','operador','consulta'];export function validateUsuario(d){const x={uid:escapeText(d.uid),nombre:escapeText(d.nombre),apellido:escapeText(d.apellido),email:normalizeEmail(d.email),telefono:escapeText(d.telefono),rol:escapeText(d.rol),activo:d.activo==='true'||d.activo===true,ultimoAcceso:d.ultimoAcceso||null,fechaInvitacion:d.fechaInvitacion||new Date().toISOString(),fechaAlta:d.fechaAlta||new Date().toISOString()};if(!x.uid||!x.nombre||!x.apellido||!x.email||!roles.includes(x.rol))throw new Error('Datos de usuario inválidos');return x}
