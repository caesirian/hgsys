import{table}from'../components/table/data-table.js';import{openModal}from'../components/modal/modal.js';
export function crudView({title,subtitle,service,columns,fields,newLabel}){const rows=service.list();return `<section><div class="toolbar"><div><h1>${title}</h1><p class="muted">${subtitle}</p></div><button class="btn primary" data-create>${newLabel}</button></div><div id="crudTable">${table(columns,rows,r=>`<button class="btn ghost" data-edit="${r.id}">Editar</button><button class="btn danger" data-del="${r.id}">Eliminar</button>`)}</div></section>`}
export function bindCrud({service,fields,rerender}){document.querySelector('[data-create]')?.addEventListener('click',()=>openModal('Nuevo registro',fields,{},data=>{service.create(data);rerender()}));document.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>{const row=service.list().find(x=>x.id===b.dataset.edit);openModal('Editar registro',fields,row,data=>{service.update(row.id,data);rerender()})});document.querySelectorAll('[data-del]').forEach(b=>b.onclick=()=>{if(confirm('¿Eliminar registro?')){service.remove(b.dataset.del);rerender()}})}
