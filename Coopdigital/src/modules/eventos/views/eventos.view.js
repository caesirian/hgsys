import{eventoService}from'../services/evento.service.js';import{table}from'../../../components/table/data-table.js';import{fmt}from'../../../utils/date.js';
export function eventosView(){const rows=eventoService.list();return `<section><h1>Eventos</h1><p class="muted">Auditoría del sistema. Solo lectura desde frontend.</p>${table([{key:'fecha',label:'Fecha',render:r=>fmt(r.fecha)},{key:'tipo',label:'Tipo',render:r=>`<span class="badge">${r.tipo}</span>`},{key:'descripcion',label:'Descripción'},{key:'usuarioId',label:'Usuario'},{key:'metadata',label:'Metadata',render:r=>JSON.stringify(r.metadata)}],rows,()=>'<span class="muted">Sistema</span>')}</section>`}
